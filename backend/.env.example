# =============================================
# PIOERP — Variáveis de Ambiente
# Copie este arquivo para .env e ajuste os valores
# =============================================

# Servidor
PORT=3000
NODE_ENV=development

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pioerp
DB_USER=postgres
DB_PASSWORD=sua_senha_aqui
DB_POOL_MAX=20
DB_IDLE_TIMEOUT_MS=30000
DB_CONNECTION_TIMEOUT_MS=2000
